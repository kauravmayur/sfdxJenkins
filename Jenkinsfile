pipeline {
    agent any
    stages {
        stage('Example Username/Password') {
            steps{
                 echo "BUILD_NUMBER = ${env.BUILD_NUMBER}"  
            }
        }
        
    }
}
